<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <link href="<%= url('https://fonts.googleapis.com/css?family=Amaranth') %>" rel='stylesheet'>
        <link rel="stylesheet" href="<%= url ('style.css') %>">
        <title>GoMakanan</title>
    </head>

    <body>
        <h1>Item List</h1>

        <div class="center-me">
            <table class="box flex">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Price</th>
                        <th>Category</th>
                        <th colspan="3">Others</th>
                    </tr>
                </thead>
                <tbody>
                    <% items.each do |item| %>
                        <tr>
                            <td><%= item.name %></td>
                            <td>Rp <%= item.price %>,00 </td>
                            <td><%= item.category.name %></td>
                        </tr>
                    <% end %>

                </tbody>
            </table>
        </div>
        <a style="color:#FDF9EA" rel="alternate" href="<%= url ('/items/create') %>"> Add New Item</a>
    </body> 
</html>