<!DOCTYPE html>
<html lang="en">
    <head>
    
        <link href="<%= url('https://fonts.googleapis.com/css?family=Amaranth') %>" rel='stylesheet'>
        <link rel="stylesheet" href="<%= url ('style.css') %>">
        <title>New Order</title>
    </head>

    <body>
        <h1 >Create New Order</h1>

        <form class="field center-me" method="POST" action="/item/add/<%= reference_no %>">

            <table class="box p">
                <th>
                    <label for="ref_no">Order ID:</label>
                     <input type="text" name="id" id="id" />
                </th>
                <th>
                    <label for="item_name">Item:</label>
                    <select id="item_name" name="name">
                        <% items.each do |item| %>
                            <option value="<%= item.id %>"><%= item.name %></option>
                        <% end %>
                    </select>
                </th>  
                <br>
            </table>
        <br>
        <button type="submit"> Submit </button>
        </form>
        <br>

    <a style="color:#FDF9EA" href="<%= uri('./list.erb') %>">List Orders</a>
    </body>
</html>
