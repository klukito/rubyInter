<!DOCTYPE html>
<html lang="en">
    <head>
    
        <link href="<%= url('https://fonts.googleapis.com/css?family=Amaranth') %>" rel='stylesheet'>
        <link rel="stylesheet" href="<%= url ('style.css') %>">
        <title>Edit Order</title>
    </head>

    <body>
        <h1 >Edit Order</h1>

        <form class="field center-me" method="POST" action="/orders/edit/<%=order.reference_no%>">

            <table class="box p">
                <th>
                    <label for="item_name">Reference Number: </label>
                    <input type="text" name="name" placeholder="Name " id="item_name" />
                </th>   
                <th>
                    <label for="item_price">Customer Name: </label>
                    <input type="text" name="price" placeholder="Price " id="item_price" />
                </th>
                <th>
                    <label for="item_category">Order Date: </label>
                    <select id="item_category" name="category">
                        <% categories.each do |category| %>
                            <option value="<%= category.id %>"><%= category.name %></option>
                        <% end %>
                    </select>
                </th>
                <br>
            </table>
        <br>
        <button type="submit"> Submit </button>
        </form>
        <br>

    </body>
</html>

<a style="color:#FDF9EA" href="<%= uri('./edit.erb') %>">List Items</a>